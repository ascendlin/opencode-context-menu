# Changelog

## 1.0.0

- Initial release.
- Add editor context menu commands for current file and selected code.
- Add terminal, clipboard, CLI, and auto integration modes.
- Use workspace-relative `@path` references for files.
- Respect native OS path separators in `@path` references.
- Add `Add Folder to OpenCode` for Explorer folder context menus.
- Move OpenCode context menu items into a dedicated menu group.
- Escape selected code as a single line in terminal mode to avoid accidental submit.
- Add configurable OpenCode CLI session options.
